 Modifier
-    .sharedElement(
-        state = rememberSharedContentState(photo.id),
+    .sharedBounds(
+        sharedContentState = rememberSharedContentState(photo.id),
         animatedVisibilityScope = animatedVisibilityScope,
     )